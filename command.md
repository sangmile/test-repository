```
$ git init
$ git status

$ touch app.py
$ git status

$ git add app.py
$ git status

$ git commit -m "Creating a simple Flask applicaiton"
$ git remote add origin https://github.com/sangmile/test-repository.git
$ git push --set-upstream origin main
```